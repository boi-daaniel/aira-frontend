export function AdminDashboardPage() {
  return (
    <section className="panel page-stack">
      <p className="eyebrow">Admin scaffold</p>
      <h2>Admin dashboard will live here.</h2>
      <p>
        This placeholder exists so the frontend structure is ready for protected admin routes once
        backend session checks are connected.
      </p>
    </section>
  );
}
